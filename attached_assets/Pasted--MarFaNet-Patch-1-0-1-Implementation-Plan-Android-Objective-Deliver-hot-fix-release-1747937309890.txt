# **MarFaNet – Patch 1.0.1 Implementation Plan (Android)**

> *Objective:* Deliver hot‑fix release **1.0.1** that addresses P0/P1 maintenance items, fortifies security pipeline, and prepares codebase for v1.1.0 feature work. Target rollout within 10 days.

---

## 1 • Scope of Patch 1.0.1

| ID    | Priority    | Description                                                                                                    | Acceptance Test                                          |
| ----- | ----------- | -------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| P0‑01 | 🔴 Critical | **CVE Patch Monitoring** – Integrate GitHub Dependabot + OWASP DC daily job; auto‑open issue on High/Critical. | CI job passes; demo auto‑created issue.                  |
| P1‑02 | 🟠 Major    | **IPv6 Rule Update Bug** – Fallback to DoH IPv4 resolver when IPv6 only.                                       | Subscription auto‑update succeeds on IPv6‑only emulator. |
| P1‑03 | 🟠 Major    | **Connection Watchdog JSON Logs** – Structured logs with disconnect reason codes.                              | Logcat shows JSON objects; unit test parses schema.      |
| P2‑04 | 🟡 Minor    | **AMOLED Pure‑Black Theme**                                                                                    | Toggle in Appearance; background #000000.                |
| P2‑05 | 🟡 Minor    | **Accessibility Fixes** – Label missing buttons; contrast ≥ 4.5:1.                                             | Accessibility Scanner score ≥ 95.                        |

---

## 2 • Timeline & Milestones

| Day | Task                                                                    |
| --- | ----------------------------------------------------------------------- |
| 0   | Create branch `hotfix/1.0.1` and tickets for each ID.                   |
| 1‑2 | Implement P0‑01 + P1‑02; push PRs; run CI.                              |
| 3   | Implement P1‑03; add schema unit tests.                                 |
| 4‑5 | Implement P2‑04 & P2‑05; UX review.                                     |
| 6   | QA regression sweep + closed beta track `1.0.1‑beta1`.                  |
| 7‑8 | Metrics monitoring (crash‑free ≥ 98 %, no regressions).                 |
| 9   | Tag `v1.0.1`; CI auto‑release APK + upload to Prod 20 % staged rollout. |
| 10  | Monitor Play Console vitals; ramp to 100 %.                             |

---

## 3 • CI Pipeline Adjustments

* **Dependabot‑triggered Workflow**: `dependabot-auto.yml` – runs OWASP DC, drops comment summary + labels.
* **Log Schema Test**: `testjsonschema.kt` ensures new JSON format stable.

---

## 4 • Risk & Mitigation

| Risk                                   | Impact | Mitigation                                          |
| -------------------------------------- | ------ | --------------------------------------------------- |
| New DoH fallback increases DNS latency | Medium | Benchmark; revert behind feature flag if Δ > 10 ms. |
| AMOLED theme asset mismatch            | Low    | Use vector icons tinted via theme overlay.          |

---

## 5 • Definition of Done

1. All five tasks merged to `release/1.0.x` with green CI.
2. Closed beta (5 days) meets quality gates.
3. Play staged rollout reaches 100 % with crash‑free ≥ 98 %.
4. CHANGELOG updated; version bumped; docs published.

---

*Ready for kickoff immediately upon approval.*
